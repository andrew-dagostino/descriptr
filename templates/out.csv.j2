{# out.csv.j2   The tempate of the Gephi CSV import file. #}
Id;Label;Source;Target;group;capacity_available;capacity_max
{% for course in courses %}
    {% if course.prerequisites %}
        {% for link in course.prerequisites.simple %}
{{ course.code }}*{{ course.number }};{{ course.code }}*{{ course.number }};{{ course.code }}*{{ course.number }};{{ link }};{{ course.group }};{{ course.capacity_available }};{{ course.capacity_max }}
        {% endfor %}
        {% for complex_link in course.prerequisites.complex %}
{{ course.code }}*{{ course.number }};{{ course.code }}*{{ course.number }};{{ complex_link | trim }};{{ course.code }}*{{ course.number }};{{ course.group }};{{ course.capacity_available }};{{ course.capacity_max }}
        {% endfor %}
    {% else %}
{{ course.code }}*{{ course.number }};{{ course.code }}*{{ course.number }};;;{{ course.group }};{{ course.capacity_available }};{{ course.capacity_max }}
    {% endif %}
{% endfor %}
